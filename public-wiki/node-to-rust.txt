3:I[7420,[],""]
6:I[2708,[],""]
7:I[9231,["185","static/chunks/app/layout-ea177f1f1aa270b2.js"],""]
8:I[6172,["601","static/chunks/app/error-0cbc5f9ddb24b3eb.js"],""]
9:I[2154,["160","static/chunks/app/not-found-c35a854c58adf9fe.js"],"Back"]
4:["wiki","public-wiki","d"]
5:["md","node-to-rust","c"]
0:["LWg_oWSEeqb4cXC5n1vGm",[[["",{"children":[["wiki","public-wiki","d"],{"children":[["md","node-to-rust","c"],{"children":["__PAGE__?{\"wiki\":\"public-wiki\",\"md\":[\"node-to-rust\"]}",{}]}]}]},"$undefined","$undefined",true],["",{"children":[["wiki","public-wiki","d"],{"children":[["md","node-to-rust","c"],{"children":["__PAGE__",{},["$L1","$L2",null]]},["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children","$4","children","$5","children"],"loading":"$undefined","loadingStyles":"$undefined","loadingScripts":"$undefined","hasLoading":false,"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","notFoundStyles":"$undefined","styles":null}]]},["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children","$4","children"],"loading":"$undefined","loadingStyles":"$undefined","loadingScripts":"$undefined","hasLoading":false,"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","notFoundStyles":"$undefined","styles":null}]]},[null,["$","html",null,{"lang":"ko","children":[["$","$L7",null,{"async":true,"src":"//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js","crossOrigin":"anonymous"}],["$","$L7",null,{"strategy":"afterInteractive","src":"https://www.googletagmanager.com/gtag/js?id=G-QKL40848FW","async":true}],["$","$L7",null,{"id":"gtag-init","strategy":"afterInteractive","dangerouslySetInnerHTML":{"__html":"window.dataLayer = window.dataLayer || [];\n  function gtag(){dataLayer.push(arguments);}\n  gtag('js', new Date());\n  gtag('config', 'G-QKL40848FW');"}}],["$","$L7",null,{"id":"ms-clarify-init","strategy":"afterInteractive","dangerouslySetInnerHTML":{"__html":"(function(c,l,a,r,i,t,y){\n  c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};\n  t=l.createElement(r);t.async=1;t.src=\"https://www.clarity.ms/tag/\"+i;\n  y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);\n  })(window, document, \"clarity\", \"script\", \"l1919qdcrc\");\n  "}}],["$","body",null,{"className":"md:text-lg lg:flex justify-center m-0 md:min-w-lg bg-gray-100","children":["$","main",null,{"className":"w-full max-w-screen-lg lg:border border-gray-800 min-h-screen","children":["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children"],"loading":"$undefined","loadingStyles":"$undefined","loadingScripts":"$undefined","hasLoading":false,"error":"$8","errorStyles":[],"errorScripts":[],"template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":["$","div",null,{"className":"flex flex-col justify-center items-center h-screen gap-8","children":[["$","h1",null,{"className":"text-8xl text-center","children":"404"}],["$","$L9",null,{"children":"뒤로 가기"}]]}],"notFoundStyles":[],"styles":null}]}]}]]}],null]],[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/css/7974a366526c0bac.css","precedence":"next","crossOrigin":""}]],"$La"]]]]
b:I[7285,["457","static/chunks/c6c50db0-730a8fdbd0bb38f0.js","401","static/chunks/fd503499-f344ff1ccbf2f791.js","254","static/chunks/254-e656a4c93afc504e.js","629","static/chunks/app/%5Bwiki%5D/%5B...md%5D/page-873fa21ef81e129f.js"],"SearchBar"]
c:I[9474,["457","static/chunks/c6c50db0-730a8fdbd0bb38f0.js","401","static/chunks/fd503499-f344ff1ccbf2f791.js","254","static/chunks/254-e656a4c93afc504e.js","629","static/chunks/app/%5Bwiki%5D/%5B...md%5D/page-873fa21ef81e129f.js"],"Breadcrumbs"]
d:I[4276,["457","static/chunks/c6c50db0-730a8fdbd0bb38f0.js","401","static/chunks/fd503499-f344ff1ccbf2f791.js","254","static/chunks/254-e656a4c93afc504e.js","629","static/chunks/app/%5Bwiki%5D/%5B...md%5D/page-873fa21ef81e129f.js"],"MermaidRenderer"]
e:I[18,["457","static/chunks/c6c50db0-730a8fdbd0bb38f0.js","401","static/chunks/fd503499-f344ff1ccbf2f791.js","254","static/chunks/254-e656a4c93afc504e.js","629","static/chunks/app/%5Bwiki%5D/%5B...md%5D/page-873fa21ef81e129f.js"],"LinkGraph"]
f:I[1018,["457","static/chunks/c6c50db0-730a8fdbd0bb38f0.js","401","static/chunks/fd503499-f344ff1ccbf2f791.js","254","static/chunks/254-e656a4c93afc504e.js","629","static/chunks/app/%5Bwiki%5D/%5B...md%5D/page-873fa21ef81e129f.js"],""]
10:T55d3,<h1 id="node-to-rust">node-to-rust</h1>
<ul>
<li><a href="https://vino.dev/blog" target="_blank">[URL] https://vino.dev/blog</a><blockquote><p> 24일 코스로 <a href="node">node</a> 숙련자들 위한 러스트 가이드</p></blockquote></li>
</ul>
<h2 id="day-1-from-nvm-to-rustup">Day 1: From nvm to rustup</h2>
<h2 id="day-2-from-npm-to-cargo">Day 2: From npm to cargo</h2>
<table>
<thead>
<tr>
<th>npm</th>
<th>cargo</th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>init</td>
<td>init</td>
<td>현재 디렉토리 기준</td>
</tr>
<tr>
<td></td>
<td>new</td>
<td>디렉토리 함께 생성</td>
</tr>
<tr>
<td></td>
<td>run</td>
<td>build + execute</td>
</tr>
<tr>
<td>install</td>
<td>fetch</td>
<td></td>
</tr>
<tr>
<td>install [package_name]</td>
<td>add</td>
<td></td>
</tr>
<tr>
<td>uninstall</td>
<td>rm</td>
<td></td>
</tr>
<tr>
<td>install --global</td>
<td>install</td>
<td>~/.cargo/bin</td>
</tr>
<tr>
<td>test</td>
<td>test</td>
<td></td>
</tr>
<tr>
<td>publish</td>
<td>publish</td>
<td></td>
</tr>
<tr>
<td>run [start]</td>
<td>run --exmaple xxx</td>
<td><a href="@todo">@todo</a></td>
</tr>
<tr>
<td>benchmarks</td>
<td>bench</td>
<td></td>
</tr>
<tr>
<td>build</td>
<td>build</td>
<td></td>
</tr>
<tr>
<td>-</td>
<td>build --release</td>
<td></td>
</tr>
<tr>
<td>clean</td>
<td>clean</td>
<td></td>
</tr>
<tr>
<td>docs</td>
<td>doc --open</td>
<td></td>
</tr>
<tr>
<td>-</td>
<td>check</td>
<td>tsc --noEmit</td>
</tr>
<tr>
<td>-</td>
<td>clippy</td>
<td>eslint</td>
</tr>
</tbody></table>
<p>npm 과 달리 task runner 를 가지지 않았다. 때문에 여전이 <a href="makefile">makefile</a> 이 사용된다.<br>저자는 <code class="span">just</code> 를 추천</p>
<pre><code class="hljs language-sh">cargo install just
</code></pre><h3 id="workspaces--monorepo">workspaces &amp; <a href="monorepo">monorepo</a></h3>
<p>Cargo.toml</p>
<pre><code class="hljs language-toml"><span class="hljs-section">[workspace]</span>
<span class="hljs-attr">members</span> = [
  <span class="hljs-string">&quot;crates/*&quot;</span>
]

<span class="hljs-section">[dependencies]</span>
<span class="hljs-attr">other-project</span> = { path = <span class="hljs-string">&quot;../other-project&quot;</span>}
</code></pre><h4 id="추가적인-툴">추가적인 툴</h4>
<ul>
<li>cargo-edit - 디펜던시 버전 변경</li>
<li><a href="cargo-workspaces">cargo-workspaces</a>(cargo ws) - lerna like</li>
<li>cargo-expand - macro</li>
<li>tomlq - jq like</li>
</ul>
<h2 id="day-3-setting-up-vs-code">Day 3: Setting up VS Code</h2>
<p>core plugin</p>
<ul>
<li>rust-analyzer</li>
<li>vadimcn.vscode-lldb</li>
<li>bungcip.better-toml</li>
<li>crates</li>
<li>search-crates-io</li>
</ul>
<h2 id="day-4-hello-world-and-your-first-two-wtfs">Day 4: Hello World (and your first two WTFs)</h2>
<p>String vs &amp;str</p>
<h2 id="day-5-borrowing--ownership">Day 5: Borrowing &amp; Ownership</h2>
<h2 id="day-6-strings-part-1">Day 6: Strings, part 1</h2>
<p>&amp;str -&gt; String 전환을 위해 .to_owned() 를 사용한다.</p>
<ul>
<li>.to_string() 은 Display trait 을 지원하기 위한 메소드</li>
<li>.into() 은 타겟타입으로의 전환을 위한 From,Into trait 의 구현</li>
</ul>
<h2 id="day-7-syntax-and-language-part-1">Day 7: Syntax and Language, part 1</h2>
<p>array는 컴파일 타임에 길이와 모든 원소가 초기화 되어야한다.
일반적으로 js 매칭되는건 VecDeque 으로 양방향 접근이 가능하다.</p>
<ul>
<li>Vec</li>
<li>VecDeque: 양쪽에서 push, pop 이 가능하다.</li>
</ul>
<h2 id="day-8-language-part-2-from-objects-and-classes-to-hashmaps-and-structs">Day 8: Language Part 2: From objects and classes to HashMaps and structs</h2>
<p>struct: 알려진 키 셋을 가진 데이터 셋의 경우
map(hashmap): 모르는 키셋 + 동일 타입</p>
<pre><code class="hljs language-rust"><span class="hljs-keyword">use</span> std::collections::HashMap;

<span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() {
  <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">map</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();
  map.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;key1&quot;</span>, <span class="hljs-string">&quot;value1&quot;</span>);
  map.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;key2&quot;</span>, <span class="hljs-string">&quot;value2&quot;</span>);

  <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;{}&quot;</span>, map.<span class="hljs-title function_ invoke__">get</span>(<span class="hljs-string">&quot;key1&quot;</span>).<span class="hljs-title function_ invoke__">unwrap_or</span>(&amp;<span class="hljs-string">&quot;&quot;</span>));
  <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;{}&quot;</span>, map.<span class="hljs-title function_ invoke__">get</span>(<span class="hljs-string">&quot;key2&quot;</span>).<span class="hljs-title function_ invoke__">unwrap_or</span>(&amp;<span class="hljs-string">&quot;&quot;</span>));
}
</code></pre><p>&quot;&quot; 의 타입은 &amp;str
<code class="span">map.get(&quot;key1&quot;)</code> 의 타입은 Option&lt;&amp;&amp;str&gt; 이 된다.
&amp;str 이고 <code class="span">get()</code> 의 경우 소유권 이전이 되면 map 에서 해당 value 가 제거되어야하기 때문에 borrowing(&amp;) 이 되야한다.</p>
<h2 id="day-9-language-part-3-class-methods-for-rust-structs--enums">Day 9: Language Part 3: Class Methods for Rust Structs (+ enums!)</h2>
<pre><code class="hljs language-rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">A</span> {
  color: <span class="hljs-type">String</span>
}
imple A {
  <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>() <span class="hljs-punctuation">-&gt;</span> {
    <span class="hljs-keyword">Self</span> {
      color: <span class="hljs-string">&quot;red&quot;</span>.<span class="hljs-title function_ invoke__">to_owned</span>(),
    }
  }
}
</code></pre><ul>
<li><code class="span">new()</code> 는 키워드가 아니라 컨벤션이다.</li>
<li><code class="span">#[derive(Debug)]</code> 구문은 Trait 의 default 구현을 제공한다.</li>
<li>함수는 기본적으로 private 이다.</li>
<li>첫번째 인자를 self 혹은 &amp;self 로 선언하면 인스턴스 메소드가 된다.<ul>
<li>self: Self</li>
<li>&amp;self: &amp;Self</li>
</ul>
</li>
<li>self 를 인자로 선언하는 경우 오너쉽을 가지게 되므로 이후 인스턴스를 잃게 된다.<ul>
<li>자바스크립의 .map() 처럼 새로운 인스턴스를 만들어서 반환하는 경우</li>
<li>소멸자와 같은 역할을 하는 경우</li>
<li>메소드 체이닝
enums 은 union type 을 처리할 수 있다.</li>
</ul>
</li>
</ul>
<h2 id="day-10-from-mixins-to-traits">Day 10: From Mixins to Traits</h2>
<p>Trait 은 자바스크립트의 Mixins 이다.
메소드의 집합이라고 생각하도록 한다.</p>
<h2 id="day-11-the-module-system">Day 11: The Module System</h2>
<ul>
<li>모든 정의는 private이 default</li>
<li>trait methods, enum variants 는 default 가 public</li>
<li>visibility 는 모듈의 것이므로 모듈 내에서는 무관하게 참조가 가능하다.</li>
<li>pub(create) 를 통해 crate 내부에서 접근 허용</li>
<li>pub(super) 부모에게만 허용</li>
</ul>
<h2 id="day-12-strings-part-2">Day 12: Strings, Part 2</h2>
<p>함수 인자는 &amp;String, String 이 아닌 &amp;str 로한다.</p>
<ul>
<li><p>String 은 Borrow<str> 이 구현되어있어 자동 컨버팅이 된다.</p>
</li>
<li><p>Display 구현함으로써 <code class="span">.to_string()</code> 을 얻을 수 있다.</p>
</li>
<li><p><code class="span">Borrow&lt;str&gt;</code> - 더 많은 가정 + 실패할 수 있다.</p>
</li>
<li><p><a href="@todo">@todo</a> <code class="span">AsRef&lt;str&gt;</code> - 더 적은 가정 + 실패할 수 없다.</p>
</li>
</ul>
<h2 id="day-13-results--options">Day 13: Results &amp; Options</h2>
<p>std::prelue 에 선언된 것들은 <code class="span">use</code> 선언 없이 사용이 가능하다.</p>
<ul>
<li><p>Some</p>
</li>
<li><p>None</p>
</li>
<li><p><code class="span">unwrap()</code> 은 Err(T), None 이 오는 경우 panic 을 발생시킨다.</p>
</li>
<li><p><code class="span">unwrap_or(v)</code> 실패 없이 v 를 기본값으로 리턴</p>
</li>
<li><p><code class="span">unwrap_or_else(|| {})</code> 실패 없이 함수를 통한 값을 리턴</p>
</li>
<li><p><code class="span">unwrap_or_default()</code> <code class="span">Default</code> trait 을 구현한 경우에 사용됨</p>
</li>
</ul>
<p>매직 value 대신 Option, Result 를 이용하자.</p>
<h2 id="day-14-managing-errors">Day 14: Managing Errors</h2>
<p>서로 다른 타입의 오류를 반환 하는 함수 작성하기</p>
<ol>
<li>Box<dyn Error></li>
</ol>
<ul>
<li>Box 는 수명 만큼 존재하는 어떤 값의 주소</li>
<li>아키텍쳐의 비트가 크기가 된다.</li>
<li>Result 는 Err 에 제한이 없으므로 Error trait 을 구현하지 않은 오류가 발생하면 실패한다.</li>
</ul>
<ol start="2">
<li>custom Error type</li>
</ol>
<ul>
<li><code class="span">impl std::error::Error for MyError {}</code></li>
<li><strong>supertraits</strong> trait 이 의존하는 trait</li>
<li><code class="span">From</code>, <code class="span">Into</code>, <code class="span">TryFrom</code>, <code class="span">TryInto</code> 구현 필요</li>
<li>From 은 타겟 타입을 보고 자동으로 Into 를 호출한다.</li>
<li><code class="span">Try*</code> 은 실패가 가능하다. 리턴 타입은 <code class="span">Result</code> 가 된다.</li>
</ul>
<ol start="3">
<li>Use crate</li>
</ol>
<ul>
<li>thiserror - 커스텀 에러, lib 에서 주로 사용</li>
</ul>
<pre><code class="hljs language-rust"><span class="hljs-meta">#[derive(thiserror::Error, Debug)]</span>
<span class="hljs-keyword">enum</span> <span class="hljs-title class_">MyError</span> {
  <span class="hljs-meta">#[error(<span class="hljs-string">&quot;Environment variable not found&quot;</span>)]</span>
  <span class="hljs-title function_ invoke__">EnvironmentVariableNotFound</span>(<span class="hljs-meta">#[from]</span> std::env::VarError),
  <span class="hljs-meta">#[error(transparent)]</span>
  <span class="hljs-title function_ invoke__">IOError</span>(<span class="hljs-meta">#[from]</span> std::io::Error),
}
</code></pre><ul>
<li>anyhow - 원본 에러를 그대로 노출, bin 에서 사용</li>
</ul>
<h2 id="day-15-closures">Day 15: Closures</h2>
<ul>
<li>Fn</li>
<li>FnMut</li>
<li>FnOnce - <strong>move</strong> 로 소유권을 잃는 경우</li>
</ul>
<p><a href="@todo">@todo</a> impl [trait] 은 함수 인자 혹은 리턴 값 이외에는 사용이 불가능하다.
때문에 dyn [trait] 으로 저장이 필요하다.
dyn [trait] 은 unsized 이며 러스트는 이를 좋아하지 않는다.</p>
<h2 id="day-16-lifetimes-references-and-static">Day 16: Lifetimes, references, and &#39;static</h2>
<p><a href="@todo">@todo</a> <code class="span">T: &#39;static</code>, <code class="span">&amp;&#39;static</code> 은 다르다
<code class="span">T: &#39;static</code> 은 영원히 지속되는 변수를 의미하지 않는다.</p>
<h2 id="day-17-arrays-loops-and-iterators">Day 17: Arrays, Loops, and Iterators</h2>
<ul>
<li>배열은 알려진 크기와 초기화되어 있어야한다.</li>
<li>자바스크립트의 Array 는 Vec 과 매칭된다.</li>
<li>VecDeque 는 양쪽에서 입출력이 가능하다.<ul>
<li>Vec -&gt; vec![]</li>
</ul>
</li>
<li><code class="span">for prop in hashmap.keys() { }</code> -&gt; <code class="span">.keys()</code> 는 임의 순서로 조정이 불가능</li>
<li><code class="span">for i in 0..max { }</code></li>
<li><code class="span">while ((data = obj.dowork())) { }</code></li>
<li><code class="span">while let Some(data) = obj.dowork() { }</code></li>
<li><code class="span">loop { break; }</code></li>
<li><code class="span">&#39;outer: loop { break &#39;outer; }</code></li>
<li><code class="span">let value = loop { break &quot;return value&quot;; }</code></li>
</ul>
<p>Iterator, Array, Vec -&gt; <code class="span">::iter()</code></p>
<ul>
<li><p>Iterator 는 lazy 하게 동작 <code class="span">collect()</code> 등의 반환 함수를 사용할때 iterator 들의 메소드 들이 사용</p>
</li>
<li><p>Vec::iter() 로 생성 or Vec::iter_mut() -&gt; Iterator 를 리턴</p>
</li>
<li><p>Iterator.collect() 로 반환 -&gt; 타겟에 타입을 지정해야함 eg, <code class="span">let a: Vec&lt;_&gt; = ...</code></p>
</li>
<li><p>Iterator.next() 반복자에서 단일 값을 얻을때 사용</p>
</li>
<li><p>Iterator.filter() 2중 참조</p>
</li>
<li><p>Iterator.find() == Iterator.filter().next(), js 와 다르게 find() 를 여러번 호출 가능</p>
</li>
<li><p>Iterator.forEach() iterator 즉시 <strong>소비</strong>, <strong>일반 루프가 선호</strong>된다.</p>
</li>
<li><p>Vec|Array.join(&quot;,&quot;) js 와 동일</p>
</li>
<li><p>Vec.push() <code class="span">Vec</code>만 가능</p>
</li>
<li><p>Vec.pop() <code class="span">Vec</code>만 가능</p>
</li>
<li><p>VecDeque.push_front() <code class="span">VecDeque</code>만 가능</p>
</li>
<li><p>VecDeque.pop_front() <code class="span">VecDeque</code>만 가능</p>
</li>
</ul>
<p><code class="span">.collect()</code> 를 통해 특정 데이터 타입을 반환하지 말고, Iterator 자체를 반환해서 지연평가와 유연성을 확보하도록 한다.</p>
<h2 id="day-18-async">Day 18: Async</h2>
<p>러스트는 러스트의 Promise 인 <code class="span">Futures</code> 를 가지고 있다. 그러나 executor, reactor(node 의 event loop) 가 존재하지 않는다.<br>이에 대한 폴리필은 아래와 같다.</p>
<ul>
<li>tokio</li>
<li>smol</li>
<li>async-std</li>
</ul>
<pre><code class="hljs language-rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">sync_fx</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> {
}
<span class="hljs-keyword">async</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">async_fx</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> { <span class="hljs-comment">// impl Future&lt;Output = String&gt;</span>
}
<span class="hljs-meta">#[tokio::main]</span>
<span class="hljs-keyword">async</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() {
  <span class="hljs-keyword">let</span> <span class="hljs-variable">msg</span> = <span class="hljs-title function_ invoke__">async_fx</span>().<span class="hljs-keyword">await</span>;
}
</code></pre><p><strong>node</strong> 와 달리 <code class="span">.await</code> 을 하기전에는 실행되지 않는다.</p>
<p>비동기 클로저</p>
<pre><code class="hljs language-rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">closure</span> = || <span class="hljs-keyword">async</span> {
    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;hello&quot;</span>);
};
</code></pre><p>쓰레드로 전송되어 사용되는 경우 인자에 경계를 주어야한다.</p>
<ul>
<li>&#39;static - 언제사용될지 모르기 때문</li>
<li>Send or Sync - Threadsafe</li>
</ul>
<h2 id="day-19-starting-a-large-project">Day 19: Starting a large project</h2>
<p><strong>cargobook</strong> 은 <code class="span">Cargo.lock</code> 을 라이브러리에서는 생략하라고 조언함(버전관리에서로 이해)
<code class="span">./Cargo.toml</code></p>
<pre><code class="hljs language-toml"><span class="hljs-section">[workspace]</span>
<span class="hljs-attr">members</span> = [<span class="hljs-string">&quot;crates/*&quot;</span>]
</code></pre><p><code class="span">./crates/my-lib/Cargo.toml</code>
<code class="span">./crates/cli/Cargo.toml</code></p>
<pre><code class="hljs language-toml">...
<span class="hljs-section">[dependencies]</span>
<span class="hljs-attr">my-lib</span> = { path = <span class="hljs-string">&quot;../my-lib&quot;</span> }
</code></pre><pre><code class="hljs language-sh">cargo run crates/cli
cargo run -p cli
</code></pre><p>test</p>
<pre><code class="hljs language-rust"><span class="hljs-meta">#[cfg(test)]</span>
<span class="hljs-keyword">mod</span> tests {
    <span class="hljs-meta">#[test]</span>
    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">it_works</span>() {
        <span class="hljs-keyword">let</span> <span class="hljs-variable">result</span> = <span class="hljs-number">2</span> + <span class="hljs-number">2</span>;
        <span class="hljs-built_in">assert_eq!</span>(result, <span class="hljs-number">4</span>);
    }
}
</code></pre><p>내부 테스트는 이와 같은 방식으로<strong>만</strong> 한다.
tests 디렉토리를 만들어서 외부에서 테스트하는 경우 <code class="span">pub</code> 접근지시자로 설정된 경우에만 접근가능하다.</p>
<p><code class="span">#cfg(flag)</code> flag 그로 on/off
<code class="span">[#test]</code> unit test -&gt; <code class="span">cargo test</code> 로 실행된다.
assert 함수</p>
<ul>
<li>assert!</li>
<li>assert_eq!</li>
<li>assert_ne!</li>
</ul>
<p>crate 이름은 하이픈(<code class="span">-</code>)을 사용하지만 코드에서 참조시에는 언더바(<code class="span">_</code>) 로 참조해야된다.</p>
<h2 id="day-20-cli-arguments--logging">Day 20: CLI Arguments &amp; Logging</h2>
<ul>
<li>structopt</li>
<li>log</li>
<li>env_logger</li>
</ul>
<pre><code class="hljs language-sh">RUST_LOG=debug cargo run -p cli
RUST_LOG=package_name=debug cargo run -p cli
</code></pre><pre><code class="hljs language-rust"><span class="hljs-keyword">use</span> structopt::{clap::AppSettings, StructOpt};

<span class="hljs-meta">#[derive(StructOpt)]</span>
<span class="hljs-meta">#[structopt(
    name = <span class="hljs-string">&quot;name&quot;</span>,
    about = <span class="hljs-string">&quot;about&quot;</span>,
    global_settings(&amp;[
      AppSettings::ColoredHelp
    ]</span>),
)]
<span class="hljs-keyword">struct</span> <span class="hljs-title class_">CliOptions</span> {
    <span class="hljs-meta">#[structopt(parse(from_os_str))]</span>
    <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">crate</span>) file_path: PathBuf,
}

<span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() {
    <span class="hljs-keyword">let</span> <span class="hljs-variable">options</span> = CliOptions::<span class="hljs-title function_ invoke__">from_args</span>();
}
</code></pre><pre><code class="hljs language-sh">cargo run -p cli -- --<span class="hljs-built_in">help</span>
</code></pre><p>cli 에 인자를 넣어서 실행하려면 <code class="span">--</code> 후에 인자를 입력한다.</p>
<p><code class="span">#[derivte(StructOpt)]</code></p>
<h2 id="day-21-building-and-running-webassembly">Day 21: Building and Running WebAssembly</h2>
<p>에러 형식을 변환하는 방법</p>
<ol>
<li>From, Into, TryFrom, TryInto</li>
<li>.map_err</li>
</ol>
<pre><code class="hljs language-rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">fx</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-title function_ invoke__">Result</span>(<span class="hljs-keyword">Self</span>, CustomError) {}
  #...
  .<span class="hljs-title function_ invoke__">map_err</span>(|e| CustomError::<span class="hljs-title function_ invoke__">Something</span>(e))?
}
</code></pre><p>웹어셈블리(WebAssembly, 이하 wa)를 지원하므로 해당 포맷으로 포맷된 파일을 읽어서 기능을 수행 할 수 있다.
wa 는 아직 완벽하지 않다. 아직 표준 스펙이 없으므로 rpc 와 같은 컨셉으로 처리를 진행한다.</p>
<ul>
<li>wapc_crate</li>
<li>wapc</li>
<li>wasmtime</li>
<li>wasmtime-proivder</li>
<li><a href="https://wapc.io" target="_blank">[URL] https://wapc.io/</a></li>
</ul>
<h2 id="day-22-using-json">Day 22: Using JSON</h2>
<ul>
<li>serde</li>
<li>serde_json - json</li>
<li>rmp_serde massage - pack</li>
</ul>
<h2 id="day-23-cheating-the-borrow-checker">Day 23: Cheating The Borrow Checker</h2>
<p>멀티 스레드 코드를 작성한다면 쓰레드간 데이터 이동을 위해 아래 두가지 Trait이 존재한다.</p>
<ul>
<li><p>Send - 불변 참조</p>
</li>
<li><p>Sync - 가변 참조</p>
</li>
<li><p>Rc - Reference Count 여러 참조가 존재하고 그 참조가 모두 제거됐을때 메모리를 해제한다.</p>
</li>
<li><p>Arc - Atomic Reference Count, <code class="span">Rc</code> 의 <code class="span">Send</code> 버전이다.</p>
</li>
<li><p>Mutex - 한 시점에 하나의 읽고 쓰기만 가능.</p>
</li>
<li><p>RwLock - Mutex 보다 무겁다. 여러 읽기가 가능하며 한 시점에 하나의 쓰기만 가능하다.</p>
</li>
<li><p><code class="span">parking_lot</code> - <code class="span">Mutex</code>, <code class="span">RwLock</code> 보다 빠르며 <code class="span">Result</code> 를 리턴하지 않는다.</p>
</li>
</ul>
<p><code class="span">Mutex</code>, <code class="span">RwLock</code> 등을 사용할때 락을 얻은 <strong>블록이 사라지면</strong> 락이 자동으로 해제된다.
조심해야할점을 락을 얻은 후에 <code class="span">.await</code> 등의 비동기 동작을 하게 되면 불필요한 락을 잡고 있게된다.</p>
<p>Tokio 는 자체 <strong>Sync</strong> crate 를 가지고 있다.</p>
<h2 id="day-24-crates--tools">Day 24: Crates &amp; Tools</h2>
<h2 id="link">link</h2>
<ul>
<li><a href="node">node</a></li>
<li><a href="rust">rust</a></li>
<li><a href="monorepo">monorepo</a></li>
</ul>
2:[["$","header",null,{"className":"flex flex-col p-3 bg-gray-800 gap-2 w-full","children":[["$","$Lb",null,{"placeholder":"$undefined"}],["$","div",null,{"className":"flex justify-between text-white gap-2","children":[["$","div",null,{"children":["$","a",null,{"target":"_blank","href":"https://github.com/deptno/deptno.dev","className":"bg-white text-black","children":"FE"}]}],["$","div",null,{"children":["$","a",null,{"target":"_blank","href":"https://github.com/deptno/public-wiki/tree/a2386f2\n","className":"bg-white text-black","children":"a2386f2"}]}],["$","a",null,{"className":"underline underline-offset-4","href":"/public-wiki/about-me","children":"about-me"}],["$","div",null,{"className":"ml-auto"}],["$","$Lc",null,{}]]}]]}],["$","div",null,{"className":"w-full overflow-x-hidden","children":[["$","$Ld",null,{}],["$","div",null,{"className":"p-2","children":["$","aside",null,{"id":"sidebar","className":"top-0 px-2 break-words border border-gray-800 3xl:text-md 3xl:border-hidden relataive flex flex-col gap-2","children":[["$","div",null,{"id":"search-result"}],[["$","div",null,{"className":"flex justify-end gap-2 border-b-2 3xl:py-2","children":[["$","a",null,{"className":"underline border-l-blue-400","href":"https://github.com/deptno/public-wiki/edit/main/node-to-rust.md","target":"_blank","children":"수정"}],["$","a",null,{"className":"underline border-l-blue-800","href":"https://github.com/deptno/public-wiki/commits/main/node-to-rust.md","target":"_blank","children":"기록"}],["$","a",null,{"className":"underline border-l-blue-800","href":"https://github.com/deptno/public-wiki/blame/main/node-to-rust.md","target":"_blank","children":"추적"}]]}],["$","$Le",null,{"wiki":"public-wiki","graphData":{"nodes":[{"id":"node-to-rust","name":"node-to-rust","val":11},{"id":"node","name":"node","val":14},{"id":"@todo","name":"@todo","val":27},{"id":"makefile","name":"makefile","val":2},{"id":"monorepo","name":"monorepo","val":4},{"id":"cargo-workspaces","name":"cargo-workspaces","val":"$undefined"},{"id":"rust","name":"rust","val":13}],"links":[{"source":"node-to-rust","target":"node"},{"source":"node-to-rust","target":"@todo"},{"source":"node-to-rust","target":"makefile"},{"source":"node-to-rust","target":"monorepo"},{"source":"node-to-rust","target":"cargo-workspaces"},{"source":"node-to-rust","target":"@todo"},{"source":"node-to-rust","target":"@todo"},{"source":"node-to-rust","target":"@todo"},{"source":"node-to-rust","target":"node"},{"source":"node-to-rust","target":"rust"},{"source":"node-to-rust","target":"monorepo"},{"source":"makefile","target":"node-to-rust"},{"source":"monorepo","target":"node-to-rust"},{"source":"rust","target":"node-to-rust"}]}}],["$","hr",null,{}],["$","div",null,{"className":"flex flex-wrap gap-2 p-0 m-0 leading-tight","children":[["$","span","node-to-rust",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/node-to-rust","children":[["$","span",null,{"className":"underline","children":"node-to-rust"}],["$","sup",null,{"children":11}]]}]}],["$","span","node",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/node","children":[["$","span",null,{"className":"underline","children":"node"}],["$","sup",null,{"children":14}]]}]}],["$","span","@todo",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/@todo","children":[["$","span",null,{"className":"underline","children":"@todo"}],["$","sup",null,{"children":27}]]}]}],["$","span","makefile",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/makefile","children":[["$","span",null,{"className":"underline","children":"makefile"}],["$","sup",null,{"children":2}]]}]}],["$","span","monorepo",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/monorepo","children":[["$","span",null,{"className":"underline","children":"monorepo"}],["$","sup",null,{"children":4}]]}]}],["$","span","cargo-workspaces",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/cargo-workspaces","children":[["$","span",null,{"className":"underline","children":"cargo-workspaces"}],["$","sup",null,{"children":"$undefined"}]]}]}],["$","span","rust",{"children":["$","$Lf",null,{"className":"text-blue-600","href":"/public-wiki/rust","children":[["$","span",null,{"className":"underline","children":"rust"}],["$","sup",null,{"children":13}]]}]}]]}],["$","hr",null,{}],["$","div",null,{"className":"toc","children":[["$","div","node-to-rust",{"className":"pl-0","children":["$","a",null,{"href":"#node-to-rust","children":"node-to-rust"}]}],["$","div","day-1-from-nvm-to-rustup",{"className":"pl-2","children":["$","a",null,{"href":"#day-1-from-nvm-to-rustup","children":"Day 1: From nvm to rustup"}]}],["$","div","day-2-from-npm-to-cargo",{"className":"pl-2","children":["$","a",null,{"href":"#day-2-from-npm-to-cargo","children":"Day 2: From npm to cargo"}]}],["$","div","workspaces--monorepo",{"className":"pl-4","children":["$","a",null,{"href":"#workspaces--monorepo","children":"workspaces & monorepo"}]}],["$","div","추가적인-툴",{"className":"pl-6","children":["$","a",null,{"href":"#추가적인-툴","children":"추가적인 툴"}]}],["$","div","day-3-setting-up-vs-code",{"className":"pl-2","children":["$","a",null,{"href":"#day-3-setting-up-vs-code","children":"Day 3: Setting up VS Code"}]}],["$","div","day-4-hello-world-and-your-first-two-wtfs",{"className":"pl-2","children":["$","a",null,{"href":"#day-4-hello-world-and-your-first-two-wtfs","children":"Day 4: Hello World (and your first two WTFs)"}]}],["$","div","day-5-borrowing--ownership",{"className":"pl-2","children":["$","a",null,{"href":"#day-5-borrowing--ownership","children":"Day 5: Borrowing & Ownership"}]}],["$","div","day-6-strings-part-1",{"className":"pl-2","children":["$","a",null,{"href":"#day-6-strings-part-1","children":"Day 6: Strings, part 1"}]}],["$","div","day-7-syntax-and-language-part-1",{"className":"pl-2","children":["$","a",null,{"href":"#day-7-syntax-and-language-part-1","children":"Day 7: Syntax and Language, part 1"}]}],["$","div","day-8-language-part-2-from-objects-and-classes-to-hashmaps-and-structs",{"className":"pl-2","children":["$","a",null,{"href":"#day-8-language-part-2-from-objects-and-classes-to-hashmaps-and-structs","children":"Day 8: Language Part 2: From objects and classes to HashMaps and structs"}]}],["$","div","day-9-language-part-3-class-methods-for-rust-structs--enums",{"className":"pl-2","children":["$","a",null,{"href":"#day-9-language-part-3-class-methods-for-rust-structs--enums","children":"Day 9: Language Part 3: Class Methods for Rust Structs (+ enums!)"}]}],["$","div","day-10-from-mixins-to-traits",{"className":"pl-2","children":["$","a",null,{"href":"#day-10-from-mixins-to-traits","children":"Day 10: From Mixins to Traits"}]}],["$","div","day-11-the-module-system",{"className":"pl-2","children":["$","a",null,{"href":"#day-11-the-module-system","children":"Day 11: The Module System"}]}],["$","div","day-12-strings-part-2",{"className":"pl-2","children":["$","a",null,{"href":"#day-12-strings-part-2","children":"Day 12: Strings, Part 2"}]}],["$","div","day-13-results--options",{"className":"pl-2","children":["$","a",null,{"href":"#day-13-results--options","children":"Day 13: Results & Options"}]}],["$","div","day-14-managing-errors",{"className":"pl-2","children":["$","a",null,{"href":"#day-14-managing-errors","children":"Day 14: Managing Errors"}]}],["$","div","day-15-closures",{"className":"pl-2","children":["$","a",null,{"href":"#day-15-closures","children":"Day 15: Closures"}]}],["$","div","day-16-lifetimes-references-and-static",{"className":"pl-2","children":["$","a",null,{"href":"#day-16-lifetimes-references-and-static","children":"Day 16: Lifetimes, references, and 'static"}]}],["$","div","day-17-arrays-loops-and-iterators",{"className":"pl-2","children":["$","a",null,{"href":"#day-17-arrays-loops-and-iterators","children":"Day 17: Arrays, Loops, and Iterators"}]}],["$","div","day-18-async",{"className":"pl-2","children":["$","a",null,{"href":"#day-18-async","children":"Day 18: Async"}]}],["$","div","day-19-starting-a-large-project",{"className":"pl-2","children":["$","a",null,{"href":"#day-19-starting-a-large-project","children":"Day 19: Starting a large project"}]}],["$","div","day-20-cli-arguments--logging",{"className":"pl-2","children":["$","a",null,{"href":"#day-20-cli-arguments--logging","children":"Day 20: CLI Arguments & Logging"}]}],["$","div","day-21-building-and-running-webassembly",{"className":"pl-2","children":["$","a",null,{"href":"#day-21-building-and-running-webassembly","children":"Day 21: Building and Running WebAssembly"}]}],["$","div","day-22-using-json",{"className":"pl-2","children":["$","a",null,{"href":"#day-22-using-json","children":"Day 22: Using JSON"}]}],["$","div","day-23-cheating-the-borrow-checker",{"className":"pl-2","children":["$","a",null,{"href":"#day-23-cheating-the-borrow-checker","children":"Day 23: Cheating The Borrow Checker"}]}],["$","div","day-24-crates--tools",{"className":"pl-2","children":["$","a",null,{"href":"#day-24-crates--tools","children":"Day 24: Crates & Tools"}]}],["$","div","link",{"className":"pl-2","children":["$","a",null,{"href":"#link","children":"link"}]}]]}],null]]}]}],["$","pre",null,{"className":"bg-gray-100 markdown p-2 whitespace-pre-wrap","dangerouslySetInnerHTML":{"__html":"$10"}}]]}]]
11:T309b,
- https://vino.dev/blog
>  24일 코스로 [[node]] 숙련자들 위한 러스트 가이드

## Day 1: From nvm to rustup
## Day 2: From npm to cargo
| npm                    | cargo             |                    |
|------------------------|-------------------|--------------------|
| init                   | init              | 현재 디렉토리 기준 |
|                        | new               | 디렉토리 함께 생성 |
|                        | run               | build + execute    |
| install                | fetch             |                    |
| install [package_name] | add               |                    |
| uninstall              | rm                |                    |
| install --global       | install           | ~/.cargo/bin       |
| test                   | test              |                    |
| publish                | publish           |                    |
| run [start]            | run --exmaple xxx | [[@todo]]          |
| benchmarks             | bench             |                    |
| build                  | build             |                    |
| -                      | build --release   |                    |
| clean                  | clean             |                    |
| docs                   | doc --open        |                    |
| -                      | check             | tsc --noEmit       |
| -                      | clippy            | eslint             |

npm 과 달리 task runner 를 가지지 않았다. 때문에 여전이 [[makefile]] 이 사용된다.  
저자는 `just` 를 추천
```sh
cargo install just
```

### workspaces & [[monorepo]]
Cargo.toml
```toml
[workspace]
members = [
  "crates/*"
]

[dependencies]
other-project = { path = "../other-project"}
```

#### 추가적인 툴
- cargo-edit - 디펜던시 버전 변경
- [[cargo-workspaces]](cargo ws) - lerna like
- cargo-expand - macro
- tomlq - jq like

## Day 3: Setting up VS Code
core plugin
- rust-analyzer
- vadimcn.vscode-lldb
- bungcip.better-toml
- crates
- search-crates-io

## Day 4: Hello World (and your first two WTFs)
String vs &str

## Day 5: Borrowing & Ownership
## Day 6: Strings, part 1
&str -> String 전환을 위해 .to_owned() 를 사용한다.
- .to_string() 은 Display trait 을 지원하기 위한 메소드
- .into() 은 타겟타입으로의 전환을 위한 From,Into trait 의 구현
## Day 7: Syntax and Language, part 1
array는 컴파일 타임에 길이와 모든 원소가 초기화 되어야한다.
일반적으로 js 매칭되는건 VecDeque 으로 양방향 접근이 가능하다.
- Vec
- VecDeque: 양쪽에서 push, pop 이 가능하다.
## Day 8: Language Part 2: From objects and classes to HashMaps and structs
struct: 알려진 키 셋을 가진 데이터 셋의 경우
map(hashmap): 모르는 키셋 + 동일 타입

```rust
use std::collections::HashMap;

fn main() {
  let mut map = HashMap::new();
  map.insert("key1", "value1");
  map.insert("key2", "value2");

  println!("{}", map.get("key1").unwrap_or(&""));
  println!("{}", map.get("key2").unwrap_or(&""));
}
```
"" 의 타입은 &str
`map.get("key1")` 의 타입은 Option<&&str> 이 된다.
&str 이고 `get()` 의 경우 소유권 이전이 되면 map 에서 해당 value 가 제거되어야하기 때문에 borrowing(&) 이 되야한다.
## Day 9: Language Part 3: Class Methods for Rust Structs (+ enums!)
```rust
struct A {
  color: String
}
imple A {
  pub fn new() -> {
    Self {
      color: "red".to_owned(),
    }
  }
}
```
- `new()` 는 키워드가 아니라 컨벤션이다.
- `#[derive(Debug)]` 구문은 Trait 의 default 구현을 제공한다.
- 함수는 기본적으로 private 이다.
- 첫번째 인자를 self 혹은 &self 로 선언하면 인스턴스 메소드가 된다.
  - self: Self
  - &self: &Self
- self 를 인자로 선언하는 경우 오너쉽을 가지게 되므로 이후 인스턴스를 잃게 된다.
  - 자바스크립의 .map() 처럼 새로운 인스턴스를 만들어서 반환하는 경우
  - 소멸자와 같은 역할을 하는 경우
  - 메소드 체이닝
enums 은 union type 을 처리할 수 있다.

## Day 10: From Mixins to Traits
Trait 은 자바스크립트의 Mixins 이다.
메소드의 집합이라고 생각하도록 한다.

## Day 11: The Module System
- 모든 정의는 private이 default
- trait methods, enum variants 는 default 가 public
- visibility 는 모듈의 것이므로 모듈 내에서는 무관하게 참조가 가능하다.
- pub(create) 를 통해 crate 내부에서 접근 허용
- pub(super) 부모에게만 허용

## Day 12: Strings, Part 2
함수 인자는 &String, String 이 아닌 &str 로한다.
- String 은 Borrow<str> 이 구현되어있어 자동 컨버팅이 된다.
- Display 구현함으로써 `.to_string()` 을 얻을 수 있다.

- `Borrow<str>` - 더 많은 가정 + 실패할 수 있다.
- [[@todo]] `AsRef<str>` - 더 적은 가정 + 실패할 수 없다.

## Day 13: Results & Options
std::prelue 에 선언된 것들은 `use` 선언 없이 사용이 가능하다.
- Some
- None

- `unwrap()` 은 Err(T), None 이 오는 경우 panic 을 발생시킨다.
- `unwrap_or(v)` 실패 없이 v 를 기본값으로 리턴
- `unwrap_or_else(|| {})` 실패 없이 함수를 통한 값을 리턴
- `unwrap_or_default()` `Default` trait 을 구현한 경우에 사용됨

매직 value 대신 Option, Result 를 이용하자.

## Day 14: Managing Errors
서로 다른 타입의 오류를 반환 하는 함수 작성하기
1. Box<dyn Error>
  - Box 는 수명 만큼 존재하는 어떤 값의 주소
  - 아키텍쳐의 비트가 크기가 된다.
  - Result 는 Err 에 제한이 없으므로 Error trait 을 구현하지 않은 오류가 발생하면 실패한다.
2. custom Error type
  - `impl std::error::Error for MyError {}`
  - **supertraits** trait 이 의존하는 trait
  - `From`, `Into`, `TryFrom`, `TryInto` 구현 필요
  - From 은 타겟 타입을 보고 자동으로 Into 를 호출한다.
  - `Try*` 은 실패가 가능하다. 리턴 타입은 `Result` 가 된다.
3. Use crate
  - thiserror - 커스텀 에러, lib 에서 주로 사용
  ```rust
  #[derive(thiserror::Error, Debug)]
  enum MyError {
    #[error("Environment variable not found")]
    EnvironmentVariableNotFound(#[from] std::env::VarError),
    #[error(transparent)]
    IOError(#[from] std::io::Error),
  }
  ```
  - anyhow - 원본 에러를 그대로 노출, bin 에서 사용

## Day 15: Closures
- Fn
- FnMut
- FnOnce - **move** 로 소유권을 잃는 경우

[[@todo]] impl [trait] 은 함수 인자 혹은 리턴 값 이외에는 사용이 불가능하다.
때문에 dyn [trait] 으로 저장이 필요하다.
dyn [trait] 은 unsized 이며 러스트는 이를 좋아하지 않는다.

## Day 16: Lifetimes, references, and 'static
[[@todo]] `T: 'static`, `&'static` 은 다르다
`T: 'static` 은 영원히 지속되는 변수를 의미하지 않는다.

## Day 17: Arrays, Loops, and Iterators
- 배열은 알려진 크기와 초기화되어 있어야한다.
- 자바스크립트의 Array 는 Vec 과 매칭된다.
- VecDeque 는 양쪽에서 입출력이 가능하다.
  - Vec -> vec![]
- `for prop in hashmap.keys() { }` -> `.keys()` 는 임의 순서로 조정이 불가능
- `for i in 0..max { }`
- `while ((data = obj.dowork())) { }`
- `while let Some(data) = obj.dowork() { }`
- `loop { break; }`
- `'outer: loop { break 'outer; }`
- `let value = loop { break "return value"; }`


Iterator, Array, Vec -> `::iter()`
- Iterator 는 lazy 하게 동작 `collect()` 등의 반환 함수를 사용할때 iterator 들의 메소드 들이 사용
- Vec::iter() 로 생성 or Vec::iter_mut() -> Iterator 를 리턴
- Iterator.collect() 로 반환 -> 타겟에 타입을 지정해야함 eg, `let a: Vec<_> = ...`
- Iterator.next() 반복자에서 단일 값을 얻을때 사용
- Iterator.filter() 2중 참조
- Iterator.find() == Iterator.filter().next(), js 와 다르게 find() 를 여러번 호출 가능
- Iterator.forEach() iterator 즉시 **소비**, **일반 루프가 선호**된다.

- Vec|Array.join(",") js 와 동일
- Vec.push() `Vec`만 가능
- Vec.pop() `Vec`만 가능
- VecDeque.push_front() `VecDeque`만 가능
- VecDeque.pop_front() `VecDeque`만 가능

`.collect()` 를 통해 특정 데이터 타입을 반환하지 말고, Iterator 자체를 반환해서 지연평가와 유연성을 확보하도록 한다.

## Day 18: Async
러스트는 러스트의 Promise 인 `Futures` 를 가지고 있다. 그러나 executor, reactor(node 의 event loop) 가 존재하지 않는다.  
이에 대한 폴리필은 아래와 같다.
- tokio
- smol
- async-std

```rust
fn sync_fx() -> String {
}
async fn async_fx() -> String { // impl Future<Output = String>
}
#[tokio::main]
async fn main() {
  let msg = async_fx().await;
}
```
**node** 와 달리 `.await` 을 하기전에는 실행되지 않는다.

비동기 클로저
```rust
let closure = || async {
    println!("hello");
};
```

쓰레드로 전송되어 사용되는 경우 인자에 경계를 주어야한다.
- 'static - 언제사용될지 모르기 때문
- Send or Sync - Threadsafe

## Day 19: Starting a large project
**cargobook** 은 `Cargo.lock` 을 라이브러리에서는 생략하라고 조언함(버전관리에서로 이해)
`./Cargo.toml`
```toml
[workspace]
members = ["crates/*"]
```
`./crates/my-lib/Cargo.toml`
`./crates/cli/Cargo.toml`
```toml
...
[dependencies]
my-lib = { path = "../my-lib" }
```

```sh
cargo run crates/cli
cargo run -p cli
```

test
```rust
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
```
내부 테스트는 이와 같은 방식으로**만** 한다.
tests 디렉토리를 만들어서 외부에서 테스트하는 경우 `pub` 접근지시자로 설정된 경우에만 접근가능하다.

`#cfg(flag)` flag 그로 on/off
`[#test]` unit test -> `cargo test` 로 실행된다.
assert 함수
- assert!
- assert_eq!
- assert_ne!

crate 이름은 하이픈(`-`)을 사용하지만 코드에서 참조시에는 언더바(`_`) 로 참조해야된다.

## Day 20: CLI Arguments & Logging
- structopt
- log
- env_logger

```sh
RUST_LOG=debug cargo run -p cli
RUST_LOG=package_name=debug cargo run -p cli
```

```rust
use structopt::{clap::AppSettings, StructOpt};

#[derive(StructOpt)]
#[structopt(
    name = "name",
    about = "about",
    global_settings(&[
      AppSettings::ColoredHelp
    ]),
)]
struct CliOptions {
    #[structopt(parse(from_os_str))]
    pub(crate) file_path: PathBuf,
}

fn main() {
    let options = CliOptions::from_args();
}
```

```sh
cargo run -p cli -- --help
```
cli 에 인자를 넣어서 실행하려면 `--` 후에 인자를 입력한다.

`#[derivte(StructOpt)]`

## Day 21: Building and Running WebAssembly
에러 형식을 변환하는 방법
1. From, Into, TryFrom, TryInto
2. .map_err
```rust
fn fx() -> Result(Self, CustomError) {}
  #...
  .map_err(|e| CustomError::Something(e))?
}
```

웹어셈블리(WebAssembly, 이하 wa)를 지원하므로 해당 포맷으로 포맷된 파일을 읽어서 기능을 수행 할 수 있다.
wa 는 아직 완벽하지 않다. 아직 표준 스펙이 없으므로 rpc 와 같은 컨셉으로 처리를 진행한다.

- wapc_crate
- wapc
- wasmtime
- wasmtime-proivder
- https://wapc.io

## Day 22: Using JSON
- serde
- serde_json - json
- rmp_serde massage - pack

## Day 23: Cheating The Borrow Checker
멀티 스레드 코드를 작성한다면 쓰레드간 데이터 이동을 위해 아래 두가지 Trait이 존재한다.
- Send - 불변 참조
- Sync - 가변 참조

- Rc - Reference Count 여러 참조가 존재하고 그 참조가 모두 제거됐을때 메모리를 해제한다.
- Arc - Atomic Reference Count, `Rc` 의 `Send` 버전이다.

- Mutex - 한 시점에 하나의 읽고 쓰기만 가능.
- RwLock - Mutex 보다 무겁다. 여러 읽기가 가능하며 한 시점에 하나의 쓰기만 가능하다.

- `parking_lot` - `Mutex`, `RwLock` 보다 빠르며 `Result` 를 리턴하지 않는다.

`Mutex`, `RwLock` 등을 사용할때 락을 얻은 **블록이 사라지면** 락이 자동으로 해제된다.
조심해야할점을 락을 얻은 후에 `.await` 등의 비동기 동작을 하게 되면 불필요한 락을 잡고 있게된다.

Tokio 는 자체 **Sync** crate 를 가지고 있다.

## Day 24: Crates & Tools




## link
- [[node]]
- [[rust]]
- [[monorepo]]
12:T309b,
- https://vino.dev/blog
>  24일 코스로 [[node]] 숙련자들 위한 러스트 가이드

## Day 1: From nvm to rustup
## Day 2: From npm to cargo
| npm                    | cargo             |                    |
|------------------------|-------------------|--------------------|
| init                   | init              | 현재 디렉토리 기준 |
|                        | new               | 디렉토리 함께 생성 |
|                        | run               | build + execute    |
| install                | fetch             |                    |
| install [package_name] | add               |                    |
| uninstall              | rm                |                    |
| install --global       | install           | ~/.cargo/bin       |
| test                   | test              |                    |
| publish                | publish           |                    |
| run [start]            | run --exmaple xxx | [[@todo]]          |
| benchmarks             | bench             |                    |
| build                  | build             |                    |
| -                      | build --release   |                    |
| clean                  | clean             |                    |
| docs                   | doc --open        |                    |
| -                      | check             | tsc --noEmit       |
| -                      | clippy            | eslint             |

npm 과 달리 task runner 를 가지지 않았다. 때문에 여전이 [[makefile]] 이 사용된다.  
저자는 `just` 를 추천
```sh
cargo install just
```

### workspaces & [[monorepo]]
Cargo.toml
```toml
[workspace]
members = [
  "crates/*"
]

[dependencies]
other-project = { path = "../other-project"}
```

#### 추가적인 툴
- cargo-edit - 디펜던시 버전 변경
- [[cargo-workspaces]](cargo ws) - lerna like
- cargo-expand - macro
- tomlq - jq like

## Day 3: Setting up VS Code
core plugin
- rust-analyzer
- vadimcn.vscode-lldb
- bungcip.better-toml
- crates
- search-crates-io

## Day 4: Hello World (and your first two WTFs)
String vs &str

## Day 5: Borrowing & Ownership
## Day 6: Strings, part 1
&str -> String 전환을 위해 .to_owned() 를 사용한다.
- .to_string() 은 Display trait 을 지원하기 위한 메소드
- .into() 은 타겟타입으로의 전환을 위한 From,Into trait 의 구현
## Day 7: Syntax and Language, part 1
array는 컴파일 타임에 길이와 모든 원소가 초기화 되어야한다.
일반적으로 js 매칭되는건 VecDeque 으로 양방향 접근이 가능하다.
- Vec
- VecDeque: 양쪽에서 push, pop 이 가능하다.
## Day 8: Language Part 2: From objects and classes to HashMaps and structs
struct: 알려진 키 셋을 가진 데이터 셋의 경우
map(hashmap): 모르는 키셋 + 동일 타입

```rust
use std::collections::HashMap;

fn main() {
  let mut map = HashMap::new();
  map.insert("key1", "value1");
  map.insert("key2", "value2");

  println!("{}", map.get("key1").unwrap_or(&""));
  println!("{}", map.get("key2").unwrap_or(&""));
}
```
"" 의 타입은 &str
`map.get("key1")` 의 타입은 Option<&&str> 이 된다.
&str 이고 `get()` 의 경우 소유권 이전이 되면 map 에서 해당 value 가 제거되어야하기 때문에 borrowing(&) 이 되야한다.
## Day 9: Language Part 3: Class Methods for Rust Structs (+ enums!)
```rust
struct A {
  color: String
}
imple A {
  pub fn new() -> {
    Self {
      color: "red".to_owned(),
    }
  }
}
```
- `new()` 는 키워드가 아니라 컨벤션이다.
- `#[derive(Debug)]` 구문은 Trait 의 default 구현을 제공한다.
- 함수는 기본적으로 private 이다.
- 첫번째 인자를 self 혹은 &self 로 선언하면 인스턴스 메소드가 된다.
  - self: Self
  - &self: &Self
- self 를 인자로 선언하는 경우 오너쉽을 가지게 되므로 이후 인스턴스를 잃게 된다.
  - 자바스크립의 .map() 처럼 새로운 인스턴스를 만들어서 반환하는 경우
  - 소멸자와 같은 역할을 하는 경우
  - 메소드 체이닝
enums 은 union type 을 처리할 수 있다.

## Day 10: From Mixins to Traits
Trait 은 자바스크립트의 Mixins 이다.
메소드의 집합이라고 생각하도록 한다.

## Day 11: The Module System
- 모든 정의는 private이 default
- trait methods, enum variants 는 default 가 public
- visibility 는 모듈의 것이므로 모듈 내에서는 무관하게 참조가 가능하다.
- pub(create) 를 통해 crate 내부에서 접근 허용
- pub(super) 부모에게만 허용

## Day 12: Strings, Part 2
함수 인자는 &String, String 이 아닌 &str 로한다.
- String 은 Borrow<str> 이 구현되어있어 자동 컨버팅이 된다.
- Display 구현함으로써 `.to_string()` 을 얻을 수 있다.

- `Borrow<str>` - 더 많은 가정 + 실패할 수 있다.
- [[@todo]] `AsRef<str>` - 더 적은 가정 + 실패할 수 없다.

## Day 13: Results & Options
std::prelue 에 선언된 것들은 `use` 선언 없이 사용이 가능하다.
- Some
- None

- `unwrap()` 은 Err(T), None 이 오는 경우 panic 을 발생시킨다.
- `unwrap_or(v)` 실패 없이 v 를 기본값으로 리턴
- `unwrap_or_else(|| {})` 실패 없이 함수를 통한 값을 리턴
- `unwrap_or_default()` `Default` trait 을 구현한 경우에 사용됨

매직 value 대신 Option, Result 를 이용하자.

## Day 14: Managing Errors
서로 다른 타입의 오류를 반환 하는 함수 작성하기
1. Box<dyn Error>
  - Box 는 수명 만큼 존재하는 어떤 값의 주소
  - 아키텍쳐의 비트가 크기가 된다.
  - Result 는 Err 에 제한이 없으므로 Error trait 을 구현하지 않은 오류가 발생하면 실패한다.
2. custom Error type
  - `impl std::error::Error for MyError {}`
  - **supertraits** trait 이 의존하는 trait
  - `From`, `Into`, `TryFrom`, `TryInto` 구현 필요
  - From 은 타겟 타입을 보고 자동으로 Into 를 호출한다.
  - `Try*` 은 실패가 가능하다. 리턴 타입은 `Result` 가 된다.
3. Use crate
  - thiserror - 커스텀 에러, lib 에서 주로 사용
  ```rust
  #[derive(thiserror::Error, Debug)]
  enum MyError {
    #[error("Environment variable not found")]
    EnvironmentVariableNotFound(#[from] std::env::VarError),
    #[error(transparent)]
    IOError(#[from] std::io::Error),
  }
  ```
  - anyhow - 원본 에러를 그대로 노출, bin 에서 사용

## Day 15: Closures
- Fn
- FnMut
- FnOnce - **move** 로 소유권을 잃는 경우

[[@todo]] impl [trait] 은 함수 인자 혹은 리턴 값 이외에는 사용이 불가능하다.
때문에 dyn [trait] 으로 저장이 필요하다.
dyn [trait] 은 unsized 이며 러스트는 이를 좋아하지 않는다.

## Day 16: Lifetimes, references, and 'static
[[@todo]] `T: 'static`, `&'static` 은 다르다
`T: 'static` 은 영원히 지속되는 변수를 의미하지 않는다.

## Day 17: Arrays, Loops, and Iterators
- 배열은 알려진 크기와 초기화되어 있어야한다.
- 자바스크립트의 Array 는 Vec 과 매칭된다.
- VecDeque 는 양쪽에서 입출력이 가능하다.
  - Vec -> vec![]
- `for prop in hashmap.keys() { }` -> `.keys()` 는 임의 순서로 조정이 불가능
- `for i in 0..max { }`
- `while ((data = obj.dowork())) { }`
- `while let Some(data) = obj.dowork() { }`
- `loop { break; }`
- `'outer: loop { break 'outer; }`
- `let value = loop { break "return value"; }`


Iterator, Array, Vec -> `::iter()`
- Iterator 는 lazy 하게 동작 `collect()` 등의 반환 함수를 사용할때 iterator 들의 메소드 들이 사용
- Vec::iter() 로 생성 or Vec::iter_mut() -> Iterator 를 리턴
- Iterator.collect() 로 반환 -> 타겟에 타입을 지정해야함 eg, `let a: Vec<_> = ...`
- Iterator.next() 반복자에서 단일 값을 얻을때 사용
- Iterator.filter() 2중 참조
- Iterator.find() == Iterator.filter().next(), js 와 다르게 find() 를 여러번 호출 가능
- Iterator.forEach() iterator 즉시 **소비**, **일반 루프가 선호**된다.

- Vec|Array.join(",") js 와 동일
- Vec.push() `Vec`만 가능
- Vec.pop() `Vec`만 가능
- VecDeque.push_front() `VecDeque`만 가능
- VecDeque.pop_front() `VecDeque`만 가능

`.collect()` 를 통해 특정 데이터 타입을 반환하지 말고, Iterator 자체를 반환해서 지연평가와 유연성을 확보하도록 한다.

## Day 18: Async
러스트는 러스트의 Promise 인 `Futures` 를 가지고 있다. 그러나 executor, reactor(node 의 event loop) 가 존재하지 않는다.  
이에 대한 폴리필은 아래와 같다.
- tokio
- smol
- async-std

```rust
fn sync_fx() -> String {
}
async fn async_fx() -> String { // impl Future<Output = String>
}
#[tokio::main]
async fn main() {
  let msg = async_fx().await;
}
```
**node** 와 달리 `.await` 을 하기전에는 실행되지 않는다.

비동기 클로저
```rust
let closure = || async {
    println!("hello");
};
```

쓰레드로 전송되어 사용되는 경우 인자에 경계를 주어야한다.
- 'static - 언제사용될지 모르기 때문
- Send or Sync - Threadsafe

## Day 19: Starting a large project
**cargobook** 은 `Cargo.lock` 을 라이브러리에서는 생략하라고 조언함(버전관리에서로 이해)
`./Cargo.toml`
```toml
[workspace]
members = ["crates/*"]
```
`./crates/my-lib/Cargo.toml`
`./crates/cli/Cargo.toml`
```toml
...
[dependencies]
my-lib = { path = "../my-lib" }
```

```sh
cargo run crates/cli
cargo run -p cli
```

test
```rust
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
```
내부 테스트는 이와 같은 방식으로**만** 한다.
tests 디렉토리를 만들어서 외부에서 테스트하는 경우 `pub` 접근지시자로 설정된 경우에만 접근가능하다.

`#cfg(flag)` flag 그로 on/off
`[#test]` unit test -> `cargo test` 로 실행된다.
assert 함수
- assert!
- assert_eq!
- assert_ne!

crate 이름은 하이픈(`-`)을 사용하지만 코드에서 참조시에는 언더바(`_`) 로 참조해야된다.

## Day 20: CLI Arguments & Logging
- structopt
- log
- env_logger

```sh
RUST_LOG=debug cargo run -p cli
RUST_LOG=package_name=debug cargo run -p cli
```

```rust
use structopt::{clap::AppSettings, StructOpt};

#[derive(StructOpt)]
#[structopt(
    name = "name",
    about = "about",
    global_settings(&[
      AppSettings::ColoredHelp
    ]),
)]
struct CliOptions {
    #[structopt(parse(from_os_str))]
    pub(crate) file_path: PathBuf,
}

fn main() {
    let options = CliOptions::from_args();
}
```

```sh
cargo run -p cli -- --help
```
cli 에 인자를 넣어서 실행하려면 `--` 후에 인자를 입력한다.

`#[derivte(StructOpt)]`

## Day 21: Building and Running WebAssembly
에러 형식을 변환하는 방법
1. From, Into, TryFrom, TryInto
2. .map_err
```rust
fn fx() -> Result(Self, CustomError) {}
  #...
  .map_err(|e| CustomError::Something(e))?
}
```

웹어셈블리(WebAssembly, 이하 wa)를 지원하므로 해당 포맷으로 포맷된 파일을 읽어서 기능을 수행 할 수 있다.
wa 는 아직 완벽하지 않다. 아직 표준 스펙이 없으므로 rpc 와 같은 컨셉으로 처리를 진행한다.

- wapc_crate
- wapc
- wasmtime
- wasmtime-proivder
- https://wapc.io

## Day 22: Using JSON
- serde
- serde_json - json
- rmp_serde massage - pack

## Day 23: Cheating The Borrow Checker
멀티 스레드 코드를 작성한다면 쓰레드간 데이터 이동을 위해 아래 두가지 Trait이 존재한다.
- Send - 불변 참조
- Sync - 가변 참조

- Rc - Reference Count 여러 참조가 존재하고 그 참조가 모두 제거됐을때 메모리를 해제한다.
- Arc - Atomic Reference Count, `Rc` 의 `Send` 버전이다.

- Mutex - 한 시점에 하나의 읽고 쓰기만 가능.
- RwLock - Mutex 보다 무겁다. 여러 읽기가 가능하며 한 시점에 하나의 쓰기만 가능하다.

- `parking_lot` - `Mutex`, `RwLock` 보다 빠르며 `Result` 를 리턴하지 않는다.

`Mutex`, `RwLock` 등을 사용할때 락을 얻은 **블록이 사라지면** 락이 자동으로 해제된다.
조심해야할점을 락을 얻은 후에 `.await` 등의 비동기 동작을 하게 되면 불필요한 락을 잡고 있게된다.

Tokio 는 자체 **Sync** crate 를 가지고 있다.

## Day 24: Crates & Tools




## link
- [[node]]
- [[rust]]
- [[monorepo]]
13:T309b,
- https://vino.dev/blog
>  24일 코스로 [[node]] 숙련자들 위한 러스트 가이드

## Day 1: From nvm to rustup
## Day 2: From npm to cargo
| npm                    | cargo             |                    |
|------------------------|-------------------|--------------------|
| init                   | init              | 현재 디렉토리 기준 |
|                        | new               | 디렉토리 함께 생성 |
|                        | run               | build + execute    |
| install                | fetch             |                    |
| install [package_name] | add               |                    |
| uninstall              | rm                |                    |
| install --global       | install           | ~/.cargo/bin       |
| test                   | test              |                    |
| publish                | publish           |                    |
| run [start]            | run --exmaple xxx | [[@todo]]          |
| benchmarks             | bench             |                    |
| build                  | build             |                    |
| -                      | build --release   |                    |
| clean                  | clean             |                    |
| docs                   | doc --open        |                    |
| -                      | check             | tsc --noEmit       |
| -                      | clippy            | eslint             |

npm 과 달리 task runner 를 가지지 않았다. 때문에 여전이 [[makefile]] 이 사용된다.  
저자는 `just` 를 추천
```sh
cargo install just
```

### workspaces & [[monorepo]]
Cargo.toml
```toml
[workspace]
members = [
  "crates/*"
]

[dependencies]
other-project = { path = "../other-project"}
```

#### 추가적인 툴
- cargo-edit - 디펜던시 버전 변경
- [[cargo-workspaces]](cargo ws) - lerna like
- cargo-expand - macro
- tomlq - jq like

## Day 3: Setting up VS Code
core plugin
- rust-analyzer
- vadimcn.vscode-lldb
- bungcip.better-toml
- crates
- search-crates-io

## Day 4: Hello World (and your first two WTFs)
String vs &str

## Day 5: Borrowing & Ownership
## Day 6: Strings, part 1
&str -> String 전환을 위해 .to_owned() 를 사용한다.
- .to_string() 은 Display trait 을 지원하기 위한 메소드
- .into() 은 타겟타입으로의 전환을 위한 From,Into trait 의 구현
## Day 7: Syntax and Language, part 1
array는 컴파일 타임에 길이와 모든 원소가 초기화 되어야한다.
일반적으로 js 매칭되는건 VecDeque 으로 양방향 접근이 가능하다.
- Vec
- VecDeque: 양쪽에서 push, pop 이 가능하다.
## Day 8: Language Part 2: From objects and classes to HashMaps and structs
struct: 알려진 키 셋을 가진 데이터 셋의 경우
map(hashmap): 모르는 키셋 + 동일 타입

```rust
use std::collections::HashMap;

fn main() {
  let mut map = HashMap::new();
  map.insert("key1", "value1");
  map.insert("key2", "value2");

  println!("{}", map.get("key1").unwrap_or(&""));
  println!("{}", map.get("key2").unwrap_or(&""));
}
```
"" 의 타입은 &str
`map.get("key1")` 의 타입은 Option<&&str> 이 된다.
&str 이고 `get()` 의 경우 소유권 이전이 되면 map 에서 해당 value 가 제거되어야하기 때문에 borrowing(&) 이 되야한다.
## Day 9: Language Part 3: Class Methods for Rust Structs (+ enums!)
```rust
struct A {
  color: String
}
imple A {
  pub fn new() -> {
    Self {
      color: "red".to_owned(),
    }
  }
}
```
- `new()` 는 키워드가 아니라 컨벤션이다.
- `#[derive(Debug)]` 구문은 Trait 의 default 구현을 제공한다.
- 함수는 기본적으로 private 이다.
- 첫번째 인자를 self 혹은 &self 로 선언하면 인스턴스 메소드가 된다.
  - self: Self
  - &self: &Self
- self 를 인자로 선언하는 경우 오너쉽을 가지게 되므로 이후 인스턴스를 잃게 된다.
  - 자바스크립의 .map() 처럼 새로운 인스턴스를 만들어서 반환하는 경우
  - 소멸자와 같은 역할을 하는 경우
  - 메소드 체이닝
enums 은 union type 을 처리할 수 있다.

## Day 10: From Mixins to Traits
Trait 은 자바스크립트의 Mixins 이다.
메소드의 집합이라고 생각하도록 한다.

## Day 11: The Module System
- 모든 정의는 private이 default
- trait methods, enum variants 는 default 가 public
- visibility 는 모듈의 것이므로 모듈 내에서는 무관하게 참조가 가능하다.
- pub(create) 를 통해 crate 내부에서 접근 허용
- pub(super) 부모에게만 허용

## Day 12: Strings, Part 2
함수 인자는 &String, String 이 아닌 &str 로한다.
- String 은 Borrow<str> 이 구현되어있어 자동 컨버팅이 된다.
- Display 구현함으로써 `.to_string()` 을 얻을 수 있다.

- `Borrow<str>` - 더 많은 가정 + 실패할 수 있다.
- [[@todo]] `AsRef<str>` - 더 적은 가정 + 실패할 수 없다.

## Day 13: Results & Options
std::prelue 에 선언된 것들은 `use` 선언 없이 사용이 가능하다.
- Some
- None

- `unwrap()` 은 Err(T), None 이 오는 경우 panic 을 발생시킨다.
- `unwrap_or(v)` 실패 없이 v 를 기본값으로 리턴
- `unwrap_or_else(|| {})` 실패 없이 함수를 통한 값을 리턴
- `unwrap_or_default()` `Default` trait 을 구현한 경우에 사용됨

매직 value 대신 Option, Result 를 이용하자.

## Day 14: Managing Errors
서로 다른 타입의 오류를 반환 하는 함수 작성하기
1. Box<dyn Error>
  - Box 는 수명 만큼 존재하는 어떤 값의 주소
  - 아키텍쳐의 비트가 크기가 된다.
  - Result 는 Err 에 제한이 없으므로 Error trait 을 구현하지 않은 오류가 발생하면 실패한다.
2. custom Error type
  - `impl std::error::Error for MyError {}`
  - **supertraits** trait 이 의존하는 trait
  - `From`, `Into`, `TryFrom`, `TryInto` 구현 필요
  - From 은 타겟 타입을 보고 자동으로 Into 를 호출한다.
  - `Try*` 은 실패가 가능하다. 리턴 타입은 `Result` 가 된다.
3. Use crate
  - thiserror - 커스텀 에러, lib 에서 주로 사용
  ```rust
  #[derive(thiserror::Error, Debug)]
  enum MyError {
    #[error("Environment variable not found")]
    EnvironmentVariableNotFound(#[from] std::env::VarError),
    #[error(transparent)]
    IOError(#[from] std::io::Error),
  }
  ```
  - anyhow - 원본 에러를 그대로 노출, bin 에서 사용

## Day 15: Closures
- Fn
- FnMut
- FnOnce - **move** 로 소유권을 잃는 경우

[[@todo]] impl [trait] 은 함수 인자 혹은 리턴 값 이외에는 사용이 불가능하다.
때문에 dyn [trait] 으로 저장이 필요하다.
dyn [trait] 은 unsized 이며 러스트는 이를 좋아하지 않는다.

## Day 16: Lifetimes, references, and 'static
[[@todo]] `T: 'static`, `&'static` 은 다르다
`T: 'static` 은 영원히 지속되는 변수를 의미하지 않는다.

## Day 17: Arrays, Loops, and Iterators
- 배열은 알려진 크기와 초기화되어 있어야한다.
- 자바스크립트의 Array 는 Vec 과 매칭된다.
- VecDeque 는 양쪽에서 입출력이 가능하다.
  - Vec -> vec![]
- `for prop in hashmap.keys() { }` -> `.keys()` 는 임의 순서로 조정이 불가능
- `for i in 0..max { }`
- `while ((data = obj.dowork())) { }`
- `while let Some(data) = obj.dowork() { }`
- `loop { break; }`
- `'outer: loop { break 'outer; }`
- `let value = loop { break "return value"; }`


Iterator, Array, Vec -> `::iter()`
- Iterator 는 lazy 하게 동작 `collect()` 등의 반환 함수를 사용할때 iterator 들의 메소드 들이 사용
- Vec::iter() 로 생성 or Vec::iter_mut() -> Iterator 를 리턴
- Iterator.collect() 로 반환 -> 타겟에 타입을 지정해야함 eg, `let a: Vec<_> = ...`
- Iterator.next() 반복자에서 단일 값을 얻을때 사용
- Iterator.filter() 2중 참조
- Iterator.find() == Iterator.filter().next(), js 와 다르게 find() 를 여러번 호출 가능
- Iterator.forEach() iterator 즉시 **소비**, **일반 루프가 선호**된다.

- Vec|Array.join(",") js 와 동일
- Vec.push() `Vec`만 가능
- Vec.pop() `Vec`만 가능
- VecDeque.push_front() `VecDeque`만 가능
- VecDeque.pop_front() `VecDeque`만 가능

`.collect()` 를 통해 특정 데이터 타입을 반환하지 말고, Iterator 자체를 반환해서 지연평가와 유연성을 확보하도록 한다.

## Day 18: Async
러스트는 러스트의 Promise 인 `Futures` 를 가지고 있다. 그러나 executor, reactor(node 의 event loop) 가 존재하지 않는다.  
이에 대한 폴리필은 아래와 같다.
- tokio
- smol
- async-std

```rust
fn sync_fx() -> String {
}
async fn async_fx() -> String { // impl Future<Output = String>
}
#[tokio::main]
async fn main() {
  let msg = async_fx().await;
}
```
**node** 와 달리 `.await` 을 하기전에는 실행되지 않는다.

비동기 클로저
```rust
let closure = || async {
    println!("hello");
};
```

쓰레드로 전송되어 사용되는 경우 인자에 경계를 주어야한다.
- 'static - 언제사용될지 모르기 때문
- Send or Sync - Threadsafe

## Day 19: Starting a large project
**cargobook** 은 `Cargo.lock` 을 라이브러리에서는 생략하라고 조언함(버전관리에서로 이해)
`./Cargo.toml`
```toml
[workspace]
members = ["crates/*"]
```
`./crates/my-lib/Cargo.toml`
`./crates/cli/Cargo.toml`
```toml
...
[dependencies]
my-lib = { path = "../my-lib" }
```

```sh
cargo run crates/cli
cargo run -p cli
```

test
```rust
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
```
내부 테스트는 이와 같은 방식으로**만** 한다.
tests 디렉토리를 만들어서 외부에서 테스트하는 경우 `pub` 접근지시자로 설정된 경우에만 접근가능하다.

`#cfg(flag)` flag 그로 on/off
`[#test]` unit test -> `cargo test` 로 실행된다.
assert 함수
- assert!
- assert_eq!
- assert_ne!

crate 이름은 하이픈(`-`)을 사용하지만 코드에서 참조시에는 언더바(`_`) 로 참조해야된다.

## Day 20: CLI Arguments & Logging
- structopt
- log
- env_logger

```sh
RUST_LOG=debug cargo run -p cli
RUST_LOG=package_name=debug cargo run -p cli
```

```rust
use structopt::{clap::AppSettings, StructOpt};

#[derive(StructOpt)]
#[structopt(
    name = "name",
    about = "about",
    global_settings(&[
      AppSettings::ColoredHelp
    ]),
)]
struct CliOptions {
    #[structopt(parse(from_os_str))]
    pub(crate) file_path: PathBuf,
}

fn main() {
    let options = CliOptions::from_args();
}
```

```sh
cargo run -p cli -- --help
```
cli 에 인자를 넣어서 실행하려면 `--` 후에 인자를 입력한다.

`#[derivte(StructOpt)]`

## Day 21: Building and Running WebAssembly
에러 형식을 변환하는 방법
1. From, Into, TryFrom, TryInto
2. .map_err
```rust
fn fx() -> Result(Self, CustomError) {}
  #...
  .map_err(|e| CustomError::Something(e))?
}
```

웹어셈블리(WebAssembly, 이하 wa)를 지원하므로 해당 포맷으로 포맷된 파일을 읽어서 기능을 수행 할 수 있다.
wa 는 아직 완벽하지 않다. 아직 표준 스펙이 없으므로 rpc 와 같은 컨셉으로 처리를 진행한다.

- wapc_crate
- wapc
- wasmtime
- wasmtime-proivder
- https://wapc.io

## Day 22: Using JSON
- serde
- serde_json - json
- rmp_serde massage - pack

## Day 23: Cheating The Borrow Checker
멀티 스레드 코드를 작성한다면 쓰레드간 데이터 이동을 위해 아래 두가지 Trait이 존재한다.
- Send - 불변 참조
- Sync - 가변 참조

- Rc - Reference Count 여러 참조가 존재하고 그 참조가 모두 제거됐을때 메모리를 해제한다.
- Arc - Atomic Reference Count, `Rc` 의 `Send` 버전이다.

- Mutex - 한 시점에 하나의 읽고 쓰기만 가능.
- RwLock - Mutex 보다 무겁다. 여러 읽기가 가능하며 한 시점에 하나의 쓰기만 가능하다.

- `parking_lot` - `Mutex`, `RwLock` 보다 빠르며 `Result` 를 리턴하지 않는다.

`Mutex`, `RwLock` 등을 사용할때 락을 얻은 **블록이 사라지면** 락이 자동으로 해제된다.
조심해야할점을 락을 얻은 후에 `.await` 등의 비동기 동작을 하게 되면 불필요한 락을 잡고 있게된다.

Tokio 는 자체 **Sync** crate 를 가지고 있다.

## Day 24: Crates & Tools




## link
- [[node]]
- [[rust]]
- [[monorepo]]
a:[["$","meta","0",{"name":"viewport","content":"width=device-width, initial-scale=1"}],["$","meta","1",{"charSet":"utf-8"}],["$","title","2",{"children":"# node-to-rust"}],["$","meta","3",{"name":"description","content":"$11"}],["$","link","4",{"rel":"author","href":"https://deptno.github.io"}],["$","meta","5",{"name":"author","content":"deptno"}],["$","meta","6",{"name":"keywords","content":"typescript,kubernetes,react,react native,terminal,neovim,vim,lua,frontend,seo,k9s"}],["$","meta","7",{"name":"referrer","content":"origin-when-cross-origin"}],["$","meta","8",{"name":"creator","content":"deptno@gmail.com"}],["$","meta","9",{"name":"publisher","content":"deptno@gmail.com"}],["$","meta","10",{"name":"apple-mobile-web-app-capable","content":"yes"}],["$","meta","11",{"name":"apple-mobile-web-app-title","content":"deptno"}],["$","link","12",{"href":"/icon.jpg","media":"$undefined","rel":"apple-touch-startup-image"}],["$","meta","13",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}],["$","meta","14",{"property":"og:title","content":"# node-to-rust"}],["$","meta","15",{"property":"og:description","content":"$12"}],["$","meta","16",{"property":"og:url","content":"https://deptno.github.io/public-wiki/node-to-rust"}],["$","meta","17",{"name":"twitter:card","content":"summary"}],["$","meta","18",{"name":"twitter:title","content":"# node-to-rust"}],["$","meta","19",{"name":"twitter:description","content":"$13"}],["$","link","20",{"rel":"icon","href":"/icon.jpg?19eda66f7295e18c","type":"image/jpeg","sizes":"400x400"}]]
1:null
